\documentclass{standalone}

\usepackage{luatex85}
\renewcommand{\familydefault}{\sfdefault}
\input figures/figures.sty

\begin{document}

    \begin{tikzpicture}[scale=\scale, every node/.style={scale=\scale}]

        \begin{scope}
            \begin{abstractclass}[text width=\mediumTextWidth]{TowerSampler}{0, 0}
                \operation{\procedure{construct}{numCandidates: \type{int}}}
                \operation{\procedure{destroy}{}}
                \operation{\procedure{reset}{numsCandidates: \type{int[:]}}}
                \operation{\procedure{getNumChoices}{}: \type{int}}
                \operation{\procedure{get}{}: \type{int}}
            \end{abstractclass}
        \end{scope}

        \begin{scope}[shift={($(TowerSampler.south) - (0 cm, 2 cm)$)}]
            \begin{abstractclass}[text width=\mediumTextWidth]{RandomCoordinates}{0, 0}
                \operation[0]{\procedure{construct}{\ldots}}
                \operation{\procedure{destroy}{}}
                \operation{\procedure{get}{iComponent: \type{int}}: \type{real[\numDimensions]}}
            \end{abstractclass}
        \end{scope}

    \end{tikzpicture}

\end{document}
